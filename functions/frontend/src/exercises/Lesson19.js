/**
 * LESSON 19: Range Checker
 * 
 * We use logical operators (&&, ||) to check multiple conditions at once.
 * 
 * WHAT TO DO:
 * 1. Create a function named 'isInRange'.
 * 2. It should take three inputs: 'num', 'min', and 'max'.
 * 3. Return true if 'num' is between 'min' and 'max' (inclusive).
 * 4. Otherwise, return false.
 * 
 * IMPORTANT: Call your function and store the result in a variable named 'rangeCheck'.
 */

// YOUR CODE HERE



// Export for validation
export const lesson19 = {
  isInRange: typeof isInRange !== 'undefined' ? isInRange : null,
  rangeCheck: typeof rangeCheck !== 'undefined' ? rangeCheck : undefined,
};
