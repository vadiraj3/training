/**
 * LESSON 26: String Transformation & Cleaning
 *
 * Formatting and sanitizing user inputs.
 *
 * WHAT TO DO:
 * Create a function 'cleanEmail' that takes a string 'email'.
 * It should trim any leading/trailing whitespace, convert it to lowercase, and return the cleaned email.
 * If the input is not a string, return an empty string "".
 *
 * IMPORTANT: Call 'cleanEmail' in index.js to see it marked as completed!
 */

// YOUR CODE HERE


// Export for validation
export const lesson26 = {
  cleanEmail: typeof cleanEmail !== "undefined" ? cleanEmail : null,
};
