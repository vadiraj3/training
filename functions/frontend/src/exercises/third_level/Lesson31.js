/**
 * LESSON 31: Sorting & Slicing Lists
 *
 * Slicing and sorting collections of data without mutating the original list.
 *
 * WHAT TO DO:
 * Create a function 'getTopThree' that takes an array of numbers 'scores'.
 * The function should sort these scores in descending order and return a new array containing the top 3 highest scores.
 * Make sure NOT to mutate the original 'scores' array (Hint: use .slice() before sorting).
 *
 * IMPORTANT: Call 'getTopThree' in index.js to see it marked as completed!
 */

// YOUR CODE HERE


// Export for validation
export const lesson31 = {
  getTopThree: typeof getTopThree !== "undefined" ? getTopThree : null,
};
