/**
 * LESSON 20: Simple Calculator
 * 
 * Functions can use multiple if statements to act as a controller.
 * 
 * WHAT TO DO:
 * 1. Create a function named 'simpleCalc'.
 * 2. It should take three inputs: 'a', 'b', and 'op' (operation string).
 * 3. If 'op' is "add", return a + b.
 * 4. If 'op' is "sub", return a - b.
 * 5. If 'op' is "mult", return a * b.
 * 
 * IMPORTANT: Call your function with (10, 5, "add") and store the result 
 * in a variable named 'calcResult'.
 */

// YOUR CODE HERE



// Export for validation
export const lesson20 = {
  simpleCalc: typeof simpleCalc !== 'undefined' ? simpleCalc : null,
  calcResult: typeof calcResult !== 'undefined' ? calcResult : undefined,
};
