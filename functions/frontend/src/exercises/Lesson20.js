/**
 * LESSON 20: Object Property Addition
 * 
 * Functions can take an object, add new information to it, and return it.
 * 
 * WHAT TO DO:
 * 1. Create a function 'addScore'.
 * 2. It takes two inputs: a 'user' object (e.g. { name: "Ali" }) and a 'score' number.
 * 3. Add the 'score' property to the object.
 * 4. Return the modified object.
 * 
 * EXAMPLE: addScore({ name: "Ali" }, 50) -> { name: "Ali", score: 50 }
 * 
 * IMPORTANT: Call your function in index.js to see it marked as completed!
 */

// YOUR CODE HERE



// Export for validation
export const lesson20 = {
  addScore: typeof addScore !== 'undefined' ? addScore : null,
};
