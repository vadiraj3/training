/**
 * LESSON 14: Password Validator
 * 
 * Many functions are used to validate data and return true or false.
 * 
 * WHAT TO DO:
 * 1. Create a function named 'isPasswordValid'.
 * 2. It should take a password string as input.
 * 3. If the password is 8 characters or longer, return true.
 * 4. Otherwise, return false.
 * 
 * IMPORTANT: Call your function and store the result in a variable named 'isValid'.
 */

// YOUR CODE HERE



// Export for validation
export const lesson14 = {
  isPasswordValid: typeof isPasswordValid !== 'undefined' ? isPasswordValid : null,
  isValid: typeof isValid !== 'undefined' ? isValid : undefined,
};
