/**
 * LESSON 16: Seconds Converter
 * 
 * Simple functions are the building blocks of larger programs.
 * 
 * WHAT TO DO:
 * 1. Create a function named 'toSeconds'.
 * 2. It should take 'minutes' as input.
 * 3. Return the total seconds (minutes * 60).
 * 
 * IMPORTANT: Call your function with 5 and store the result 
 * in a variable named 'seconds'.
 */

// YOUR CODE HERE



// Export for validation
export const lesson16 = {
  toSeconds: typeof toSeconds !== 'undefined' ? toSeconds : null,
  seconds: typeof seconds !== 'undefined' ? seconds : undefined,
};
