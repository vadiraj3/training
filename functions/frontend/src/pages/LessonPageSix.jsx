import { Button } from "@mui/material";
import React from "react";

const LessonPageSix = () => {
  return (
    <Button onClick={() => console.log("Inline function executed!")}>
      Click Me
    </Button>
  );
};

export default LessonPageSix;
