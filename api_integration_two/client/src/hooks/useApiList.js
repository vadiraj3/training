/**
 * Optional pattern for learners:
 * keep fetch/loading/error logic reusable instead of repeating in every page.
 *
 * TODO: implement this yourself if you want a shared hook.
 * Example idea:
 * - accept a fetcher function
 * - return { data, loading, error, reload }
 */
export function useApiList() {
  throw new Error('TODO: implement useApiList in hooks/useApiList.js');
}
